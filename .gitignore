# Variables d'environnement locales (contiennent des secrets)
docker/.env

# Sauvegardes binaires volumineuses
openwebui/backup/*.db
openwebui/backup/*.sqlite
openwebui/backup/*.tar.gz

# OS
.DS_Store
Thumbs.db
